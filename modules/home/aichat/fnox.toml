#:schema https://fnox.jdx.dev/schema.json

# op-personal is defined in modules/home/gh/fnox.toml; providers are
# global to fnox.toml and declaring one twice is a duplicate key. A
# named profile, so it resolves on demand rather than on every shell
# start.
[profiles.aichat.secrets]
CLAUDE_API_KEY = { provider = "op-personal", value = "op://Private/zwcgcrmzybmgmpxeo3usbnw67u/credential" }
